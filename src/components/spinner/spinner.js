import React from 'react';
import Spinner_base from "./Spinner_base"
import "./style.css"

const Spinner = () => {
    // return <div className="spinner">loading...</div>
    return (
        <div>
            <Spinner_base/>
        </div>
    );
}

export default Spinner;